'use strict';

/**
 * suscription-payment router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::suscription-payment.suscription-payment');
