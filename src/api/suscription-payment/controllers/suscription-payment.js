'use strict';

/**
 * suscription-payment controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::suscription-payment.suscription-payment');
