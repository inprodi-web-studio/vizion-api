'use strict';

/**
 * contact-interaction router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::contact-interaction.contact-interaction');
